server.port=8080

# Thymeleaf default (templates in classpath:/templates/ con suffisso .html)
spring.thymeleaf.check-template-location=true

# OpenAI (se presente usa la chiave; altrimenti la chat farà fallback minimale)
openai.api.key=${OPENAI_API_KEY:}

# Se passi a OLLAMA locale, puoi aggiungere:
# ollama.base-url=http://localhost:11434
# ollama.model=llama3.1:8b
